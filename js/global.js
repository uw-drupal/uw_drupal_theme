/**
 * @file
 * Global utilities.
 *
 */
(function (Drupal) {
  'use strict';

  Drupal.behaviors.uw_drupal_theme = {
    attach: function (context, settings) {
      // console.log('this file is firing');
    },
  };
})(Drupal);
