{#
/**
 * @file
 * UW Drupal Theme override for page titles.
 *
 * Available variables:
 * - title_attributes: HTML attributes for the page title element.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * Provided by uw_drupal_theme:
 * - uw_hero_template_type: The type of hero selected (default, big_hero, small_hero, etc.).
 *
 * @see template_preprocess_page_title()
 */
#}
{% if uw_hero_template_type not in ['big_hero', 'small_hero', 'jumbotron', 'no_title'] %}
  {{ title_prefix }}
  {% if title %}
    <header class="entry-header">
      <h1{{title_attributes.addClass('entry-title')}}>{{ title }}</h1>
    </header>
  {% endif %}
  {{ title_suffix }}
{% endif %}
